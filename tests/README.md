# Verification scripts

These drive a real Blender in background mode against a real Sollumz install.
Nothing is mocked: every check runs the actual operators and reads the actual
mesh data back, which is the only way to catch the things that go wrong here —
Sollumz renaming a helper, a shader parameter moving, an attribute landing on the
wrong domain.

```bash
python tests/run_all.py --blender "D:/Blender52/blender.exe"
blender -b --python tests/verify_decal_tool.py          # one script on its own
```

**Use the runner once Void Tools is installed from the extension repository.**
The copy Blender has enabled is then `bl_ext.<repo>.void_tools` while these
scripts import the working tree - two module objects for the same code, so the
panels a test finds are the installed ones and the state it pokes at is the
repo's, and suites fail for reasons that have nothing to do with the code.
Registering the repo copy on top of the installed one is worse: the same
classes twice. `run_all.py` stands the installed copy down for each run and
gives the repo copy the preferences entry it would otherwise lack, both in
memory - `default_set=False`, preferences never saved, so the install is
exactly as it was when the run ends.

Each script prints one `[PASS]`/`[FAIL]` line per check, ends with
`RESULT: n/n checks passed`, and exits non-zero if anything failed.

| Script | Covers |
| --- | --- |
| `verify_decal_tool.py` | the Decal Tool end to end — surface alignment under rotated and non-uniformly scaled sources, library scanning, coplanar merge, surface walking, thumbnails, failure cleanup |
| `border.py` | the decal's border ring: grid shape, alpha-0 outer edge, per-side Border Alpha, Edge Fade |
| `corner_alpha.py` | per-corner alpha on the inner rectangle, and that it can never resurrect the border |
| `params.py` | the shader value parameters each tool writes, and that the tools do not share them |
| `bundled.py` | each tool's `textures/` folder, colour space, embedded flag, and material separation |
| `vcolor.py` | the shared `Color 1` and shade-smooth, across all four tools |
| `panels.py` | **every** Seto panel's `draw()`, driven by hand against a validating stub layout, with Sollumz both available and missing. Blender only draws from the UI thread, so nothing else here would catch a panel that explodes on first redraw. Also the tab's layout contract (`shared/panel_layout.py`): that every settings child polls for Sollumz and declares its own `bl_order`, and that each tool's "Selected X" panel is last and open |
| `ground_strip.py` | Ambient Occlusion's **Ground Level** mode: that the strip runs along where the mesh crosses a world height and goes up only, that the source mesh is not touched, that the height survives a rotated and non-uniformly scaled object, and that an object nowhere near the level says so |
| `sollumz_detect.py` | finding Sollumz whatever it is called — a legacy folder, an extension, a GitHub branch archive, a development build, or a name nobody anticipated — plus the manual override, and that a fork missing Sollumz's `dependencies` module is still usable. `resolve()` is pure, so forks nobody here has installed are testable |
| `strip_settings.py` | the strip shape the Geometry section owns for Edge Wear and Smooth Edge: that no setting is offered twice down the tab, that a value typed into the section reaches both tools' operators and lands on the strip, and that a finished strip is never retro-fitted when the section changes afterwards |
| `icons.py` | the add-on's own panel icons — that the PNGs ship, decode at 32×32, and that each header asks for its own rather than a built-in; plus the enums that must be greyed out when the library behind them is empty. `preview.icon_id` is always 0 in background mode, so the id itself cannot be checked here |
| `smoothedge.py` | Smooth Edge specifically |
| `fake_ao_alpha.py` | Ambient Occlusion's **Alpha Bottom/Top**: that 1.0 at both ends is byte-for-byte a no-op, that the fade multiplies the across-fade rather than replacing it, that it follows world up on a rotated source, and that a run with no height is left alone instead of having its shelf faded |
| `fake_ao_bevel.py` | Ambient Occlusion's live Bevel: that the source mesh is modified rather than edited, that the round reaches the *evaluated* mesh Sollumz exports and matches the strip's, that switching it off puts the source back, that one modifier serves several strips at each one's own width, and that bevel weights land on strip edges and nowhere else |
| `pinned_position.py` | that a hand-moved strip stays where it was put, on all four strip tools: that Pin Position adopts the drag, that a rebuild no longer snaps the strip back onto its source, that repeated rebuilds accumulate no drift, that a setting which legitimately moves the strip carries the offset along rather than freezing it, and that Clear Offset returns it |
| `edge_dirt.py` | Edge Dirt, which shares Ambient Occlusion's geometry and rebuild: that a dirt strip carries its own per-object data and never claims to be an AO strip, that the shared rebuild reads the group it is handed, that the two Scene settings and materials stay apart, and that the rebuild guard is one flag for both |
| `surface_painter.py` | Surface Painter: that the paint mesh's UVs are one planar projection (an island would draw the decal twice), that dragging and wheel-resizing leave what you grabbed under the pointer at any size and rotation, and that Optimize crops and welds without moving the image |
| `updater.py` | the in-Blender updater: that the version maths pins garbage tags to "not newer", that the installable zip is picked over GitHub's source archives, that installing before checking and installing from any host but this repository's releases are both refused, and — the terms themselves — that the updater package is the only code that can reach the network, that the once-per-start check is the only thing on a timer, honours the preference that turns it off, and never runs in background Blender |
| `support.py` | the bug report: that the body carries the three questions and the versions a maintainer asks for first, that the versions are the real ones, that the URL is GitHub's own new-issue form with the fields round-tripping through the query string intact, that a report too long for a URL falls back to the clipboard rather than being truncated, and — the point of the whole design — that nothing in the support code can reach the network on its own: the add-on fills the form in, the user submits it |
| `density_checker.py` | the Density Check: that the √area budget maths pins and interpolates where it should, that the measurement is taken from the evaluated mesh in world space (a modifier or a scale changes the verdict), that Analyze writes only the object colour and ID properties, that Finish restores the user's own colour even after two runs, and that starting Texture Budget over a live density run hands back the *user's* colour rather than the green painted on top of it |
| `texture_budget.py` | the Texture Budget: that texel density is measured against world area, that the scene total counts a shared texture once (forty props wearing one sheet stream it once), that an untextured mesh is greyed rather than failed, that the suggested size is a power of two at the target, and that Finish returns every colour |
| `preflight.py` | Pre-Flight: that each check catches its own failure and a clean object comes back with nothing said about it, that an n-gon and a mesh with no material are deliberately *not* reported while a PNG texture is (and a packed image with no file on disk is not), that non-uniform scale is graded worse than uniform (normals and tangents disagree only under it), that every check has fix steps written for it, that only the four mechanical repairs have a fixer and each actually repairs its own finding, that a fixer refuses with a reason where acting would be wrong (an empty slot faces still point at) and that the reason lands on the row rather than only in the status bar, that confirming the shared-mesh dialog once settles every row waiting on it and makes only the copies actually needed, that a fake user is not mistaken for a second object, that every fixable row sorts above every other one and each group's heading is drawn once rather than split by a severity that differs inside it, that a finding inside one material carries that slot and jumping to it selects it, that Fix All clears the mechanical findings and leaves the decisions untouched, that checking fixes nothing behind the user's back, and that an all-clear run is distinguishable from no run at all |
| `scatter.py` | Trash Scatter: that the catalogue's names, bounds and weights hold together, that one seed reproduces one layout, that spacing is honoured and Edge Bias measurably pulls the layout to the region boundary, that proxies ground their bounding box on the floor and register as MLO entities in the room whose bounds hold them — never limbo, whose entity cap Sollumz 2.9 enforces with a popup that is a crash in background mode — that a second run replaces the first, that Clear removes proxies and entity rows together, that Edit Mode scatters only the selected faces, that the floor's own settings rebuild the layout live (and hold still with Live Update off until Re-Scatter), that a prop library .blend dresses its props with the real mesh — appended once, shared by every instance — while missing props stay wireframe boxes, that the file's only MLO archetype is found without selection help, and the Floor Dirt overlay: deterministic bounded noise, grime measurably pooling along the boundary, exactly one parented overlay sheet floated millimetres above the floor with a varying Color 1 alpha pattern, removed live at Amount 0 and by Clear; plus Topple (standing props lie on their rolled bounding box at 1, stand again at 0, flat props never topple), Optimize Dirt genuinely reducing the overlay's face count, the dirt field measurably pulling litter to where it is dark, the obstacle ray keeping every sample out from under a table while the rest of the floor still fills, Clustering measurably heaping the layout (count-normalised nearest-neighbour distance — raw distance grows with sparsity and hides the heaps) while staying seeded, the rim of two unwelded slabs excluding their shared seam, the boundary index agreeing with an exhaustive scan to within a nanometre, a tightened Optimize tolerance measurably keeping more of the mesh than the default, and a library shaped the way Sollumz's Asset Library builder can leave a Drawable — the archetype name on an empty, the mesh parented under it — still yielding the mesh rather than a box |
| `vertex_bake.py` | Vertex Color Bake, the one tool that writes to your mesh: that it lives in this add-on's namespace rather than the `MLOPT_*` one it arrived with (a class name belonging to another add-on registers twice the day a user has both, and the `SETO_PT_` prefix is how the suite finds panels at all), that a bake **never touches `Color 1`'s alpha** — the channel the decal shaders blend by — that it writes a per-corner byte colour only on the objects you selected, that its layers measurably darken an occluded corner and repeat exactly on a second run, that strength 0 gives the base colour back, and that changing any setting bakes the selection at once with no button pressed; plus the additions that arrived with [#3](https://github.com/seto3d/void-tools/pull/3) — that Clear takes the colour back without touching the alpha, that a collection is a target on its own with hidden objects opt-in while Live Update stays on the selection whatever the target is, that the two-colour gradient lands its top and bottom colours, and that the cache signature follows the object's transform (world-space AO and shadow are wrong the moment it does not) |
| `extension.py` | the Blender extension package and the repository listing that serves it: that the manifest sits at the archive root with the version `bl_info` reports (a listing advertising a version the add-on does not report is an update that arrives and changes nothing), that no entry name carries a backslash, that the listing points at the release asset by absolute URL rather than at the docs site, and — the part only a real install can show — that Blender enables it as `bl_ext.user_default.void_tools`, that the tab and its scene settings come up under that name, that preferences resolve through the package rather than a hardcoded one, and that the add-on's own updater recognises Blender is doing the updating and reports the right version while doing it. Needs `scripts/build_extension.py` to have run; it says so and stops if not. It disables the legacy copy for the duration and puts it back, because both register the same classes |
| `texture_repair.py` | that a bundled texture whose scene still names the old install (`seto_tools/…`) is re-pointed at the copy this add-on ships when the file is opened — and, just as much, what the repair refuses: a missing texture belonging to another add-on, a missing file not shaped like one of ours, and any image that opens. Also that the handler is `@persistent` (asked with `hasattr` — Blender marks it by setting the attribute to **None**, so a truthiness test says it is not) and that nothing in the module writes to disk |
| `shadow_map.py` | the Shadow Map Baker's Cycles check, which has now shipped wrong twice in opposite directions: that `engine`'s enum items really do list only EEVEE whether Cycles is enabled or not and whether asked of the type or of a live scene (so the membership test could not have worked, and refused every bake on every machine), that assigning an unregistered engine *raises* rather than being quietly ignored (so assign-then-compare never reaches its own error message), that the replacement lets a bake start where Cycles is enabled, refuses where it is not, and follows the add-on being switched back on without a restart, and that the message points at Get Extensions rather than an Add-ons list which has had no Cycles entry since 4.2. Plus what the panel says when a run goes wrong: that a degraded bake warns instead of printing to a console Blender does not open (the shape that hid the denoise bug for a release), that the warning survives to the end of the bake rather than being overwritten by the Sollumz result, that it is a warning and not an error because the map is still usable, and that a message cannot become a tombstone - it is dismissable and every run clears it on the way in |
| `sign_glow.py` | Sign Glow, the halo behind lit lettering (contributed by Molo Modding): that it arrived out of its old `MOLO_*` namespace, that the silhouette is genuinely traced rather than left an empty rectangle, that the tight core and the wide bloom are two separate layers and dropping one narrows the glow, that alpha carries the same value the colour does — the emissive shaders read coverage from it — that the plane lands behind the letters in their own collection with the source mesh untouched, that it is a bordered grid whose outer ring carries alpha 0 (the emissive shader blends by that alpha, so a plane at 1.0 to its border is a lit box of air around the sign) and that widening Edge Fade moves the band inwards rather than lifting the edge off 0, that Create writes **nothing** to disk — the halo stays packed until Export or Save DDS is pressed, and only then does the rebuild keep the file equal to the sliders, that every setting rebuilds live and holds still with Live Update off, that the rebuild reaches for `bpy.ops` nowhere (asked of the syntax tree, since the module's own docstring says so in prose), and the `.dds` bytes this add-on has to write itself because Blender cannot: the magic, the 124-byte header, BGRA order and top-down rows — the two mistakes that produce a file which opens fine and looks wrong |
| `drawable_uv.py` | Drawable-collection placement, and Edge Wear's UV scale/offset |
| `verify_coexist_export.py` | all tools registered together, their collections, and a real Sollumz YDR export |

## Run them against every Blender you support

Sollumz's API differs between versions, and so does its exporter — 2.9.0 dropped
the CodeWalker-XML exporter that 2.8.x has, for instance. A green run on one
version proves nothing about the other.

```bash
"/c/Program Files/Blender Foundation/Blender 5.0/blender.exe" -b --python tests/verify_decal_tool.py
"/d/Blender52/blender.exe" -b --python tests/verify_decal_tool.py
```

## Two traps worth knowing

**The scripts import the *installed* add-on, not this folder.** They add the repo
to `sys.path`, but if `void_tools` is already enabled in that Blender, Python
resolves the installed copy first. After changing code, copy it into
`scripts/addons/void_tools` **and verify the copy actually landed** before
trusting a run — a sync that silently fails looks exactly like a code regression.

**`bpy.ops` raises when an operator reports `{'ERROR'}`.** That is normal for
script calls; in the UI it is just a red status message. The scripts translate it
back into a return value rather than letting it abort the run.
